spring:
  application:
    name: driver-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/drivers
    username: postgres
    password: postgres

  liquibase:
    change-log: db/changelog/main-changelog.xml

server:
  port: 8085
